using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//$TODO - Implement Boat Management Controls
public class FloatillaBoatManagementControls : MonoBehaviour
{
    [SerializeField]
    Camera OrthoCamera;

    [SerializeField]
    Camera MainCamera;

    [SerializeField]
    Floatilla FloatillaReference;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
